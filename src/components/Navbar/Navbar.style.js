import styled from 'styled-components';

export const StyledNavbar = styled.div`
	border: 1px solid white;

	position: absolute;
	top: 0;
	left: 0;
	width: 100%;
	display: flex;
	flex-direction: row;
	gap: 20px;
`;
